def add(width: int, height: int):
    """Returns width and height options for a given scene

    Args:
        width (int): width of the scene
        height (int): height of the scene
    Returns:
        the option string
    """
    return f"+W{width} +H{height}"
